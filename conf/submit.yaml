defaults:
  - base_submit
  - hydra/logging
  - model: cnn1d_bb

# Use this when uploading to Kaggle

###############
#             #
#    PATHS    #
#             #
###############

# result_path: /kaggle/working/
result_path: submission/

# Local paths
data_path: data/shrunken
submission_path: data/submission.csv

# Replace known/unkown building block predictions with 0
replace_predictions: 'known' # 'unknown', 'none'
