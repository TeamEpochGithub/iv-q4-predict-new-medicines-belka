defaults:
  - pipeline/default@_here_
  - _self_

x_sys:
  steps: []

y_sys:
  steps: []

train_sys:
  steps:
    - _target_: src.modules.training.molecule_tokenizer.MoleculeTokenizer
      training: "5M"
      window_size: 7


pred_sys:
  steps: []

label_sys:
  steps: []
